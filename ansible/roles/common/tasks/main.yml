---
- name: Create codis install directory
  file: path={{ codis_install_dir }} state=directory owner=work group=work

- name: Create codis bin directory
  file: path={{ codis_install_dir }}/bin state=directory owner=work group=work

- name: Create codis conf directory
  file: path={{ codis_install_dir }}/config state=directory owner=work group=work

- name: Create codis admin directory
  file: path={{ codis_install_dir }}/admin state=directory owner=work group=work
